# SRP App Builder Web

A full-stack IDE for decomposing GitHub repositories into SRP components and assembling them into Kivy-based Android APKs.

## Architecture
- **Frontend**: React + TypeScript + Tailwind CSS
- **Backend**: Flask JSON API
- **Core**: AST-based code splitting and SQLite symbol indexing
- **Deployment**: Cloud-native APK pipeline via GitHub Actions
